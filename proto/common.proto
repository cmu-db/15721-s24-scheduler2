syntax = "proto3";

package composable_database;

// Common message types and enums here

message TaskID {
    uint64 query_id = 1;
    uint64 task = 2;
}

enum QueryStatus {
    DONE = 0;
    IN_PROGRESS = 1;
    FAILED = 2;
    NOT_FOUND = 3;
}

// Other common definitions
